# OCR 识别

## 任务方法

使用 RapidOCR ONNX 在 CPU 上识别上传图片中的文字：

1. 校验资料存在、属于当前项目且为受支持的图片格式。
2. 调用 `local.ocr.rapidocr` 识别文字区域；识别结果作为分块写回存储，
   替换该资料此前的 OCR 分块，并更新资料状态。
3. 返回拼接文本、逐区域 Evidence、区域数量与耗时。

## 边界

- 仅处理已上传图片；扫描 PDF 需先导出图片。
- 全部在本地 CPU 执行，不发起网络请求。
