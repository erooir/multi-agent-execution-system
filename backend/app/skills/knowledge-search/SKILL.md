# 知识库检索

## 任务方法

在授权范围内的项目资料中定位可引用的文档片段：

1. 接收查询词、可选项目范围与可选的显式资料清单。
2. 调用 `local.knowledge.keyword_search` 在本地 SQLite 分块库中做关键词打分检索。
3. 返回按相关度排序的 Evidence 列表，每条 Evidence 必须能定位到真实分块。

## 边界

- 只读本地存储，不发起任何网络请求。
- 仅本次任务显式选中的临时资料参与检索；资料库范围的检索不会带入临时上传。
- 检索不到时返回空列表，不得编造片段。
